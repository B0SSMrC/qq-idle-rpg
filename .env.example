DRIVER=~fastapi+~httpx+~websockets
# 让"注册""探索"等裸关键词指令(无 / 前缀)也能匹配;配合 to_me() 仍要求 @机器人
COMMAND_START=["", "/"]
# 从 QQ 开放平台(q.qq.com)开发设置获取:复制本文件为 .env 再填入。
# 密钥只放 .env(已被 .gitignore 忽略),切勿写进本模板文件。
QQ_BOTS='[{"id": "你的AppID", "token": "你的Token", "secret": "你的AppSecret"}]'
